
public interface Leasable {
//no field, constructor, only abstract methods
	
	//if sth implements interface, it has to have all methods of interface
	
	int getCost();
	//names given to interface often sounds adjectives
	//b/c interface suppplies additional characteristics to a class
	//e.g apple implements edible-> apple is edible

}
